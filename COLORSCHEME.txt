Primary (backgrounds):             #263640   dark slate
Secondary (text, headings):        #ffffff   white

Accent blue (graphs):              #5096c0
Accent red (graphs):               #e87878
Accent yellow (graphs):            #e9c46a


SEMANTIC ASSIGNMENTS (2026-09-21)
=================================

One role, one colour, across every figure and slide. The three accents above are spent
first, on the roles that recur most; the rest come from connectome_snns.visualization,
where every value below is defined. Figures never hardcode a colour -- see
common/style.py (cell type, teacher/student) and common/plotting.py (perturbation).

Cell type, in the rate and delta scatters
  Excitatory                       #e87878   accent red        SLIDE_RED
  Inhibitory                       #5096c0   accent blue       SLIDE_BLUE

Source, wherever the two are drawn together (rasters, mean delta rate)
  Teacher (ground truth)           #263640   dark slate        SLIDE_INK
  Student (the model)              #e9c46a   accent yellow     SLIDE_YELLOW

Population, in the sweeps
  Observed                         #1d3557   navy              OBSERVED_COLOR
  Unobserved / held-out            #e9a23b   amber             UNOBSERVED_COLOR

Figure 2's variants, one fixed colour each, never reassigned
  Full connectome                  #457b9d   steel blue        FULL_CONNECTOME_COLOR
  Learnt recurrence                #e07b2e   orange            LEARNT_RECURRENCE_COLOR
  Shuffled weights                 #2a9d8f   teal              SHUFFLE_WEIGHTS_COLOR
  Shuffled topology                #8e6bb3   violet            CONFIGURATION_MODEL_COLOR

Figure 4's error models
  Neuron removal                   #b5446e   raspberry         NEURON_REMOVAL_COLOR
  Synapse dropout                  #3c8d5a   green             SYNAPSE_DROPOUT_COLOR

Figure 5
  Weight noise                     #e76f51   coral             WEIGHT_NOISE_COLOR

References and furniture
  Noise ceiling (dotted)           #404040   grey              LEGEND_GREY
  Teacher dimensionality marker    #7d8597   slate grey        DIMENSIONALITY_COLOR
  Targeted cells (triangles)       #555555   grey              TARGETED_COLOR
  Raster bands                     #eeeeee   near-white        RASTER_BAND_COLOR

Resolved on 2026-09-21: #457b9d had meant inhibitory AND student AND full connectome;
#e76f51 had meant excitatory AND teacher; teal had meant shuffled weights AND the
dimensionality marker. Cell type and source now use the deck's own accents, the marker
has its own grey, and targeted cells are identified by marker shape rather than colour.

Open: the panels are built on a white background with grey gridlines, not on the dark
slate above. If the slides are dark, the ink teacher colour and the grey furniture need
re-tuning.
